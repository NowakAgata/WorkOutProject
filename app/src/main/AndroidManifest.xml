<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.workoutproject">

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartTrainingActivity"></activity>
        <activity android:name=".TrainingViewActivity" />
        <activity android:name=".TrainingWorkoutDetailsActivity" />
        <activity android:name=".NewTrainingActivity" />
        <activity android:name=".WorkoutViewActivity" />
        <activity android:name=".ListOfOnePartExercisesActivity" />
        <activity android:name=".NewExerciseAcitivity" />
        <activity android:name=".ProfileSettingsActivity" />
        <activity android:name=".ListOfTrainingsActivity" />
        <activity android:name=".ListOfExercisesActivity" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".Logging_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>